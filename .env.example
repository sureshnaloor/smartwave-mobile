# API base URL. Used when running the app (expo start, etc.).
# Production:
# EXPO_PUBLIC_API_URL=https://www.smartwave.name
# Local dev (use your Mac's IP; simulator cannot use localhost):
# EXPO_PUBLIC_API_URL=http://192.168.1.5:3000
EXPO_PUBLIC_API_URL=https://www.smartwave.name
