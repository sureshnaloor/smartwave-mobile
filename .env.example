# API base URL. Used when running the app (expo start, etc.).
# EXPO_PUBLIC_API_URL=https://www.smartwave.name
# Local dev (same Wi‑Fi): EXPO_PUBLIC_API_URL=http://192.168.1.5:3000
# Local dev + Google sign-in: use ngrok and set EXPO_PUBLIC_API_URL=https://xxxx.ngrok.io (no trailing slash)
EXPO_PUBLIC_API_URL=https://www.smartwave.name

# Google OAuth Web client ID (same as web app GOOGLE_CLIENT_ID). Required for "Continue with Google".
# EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
